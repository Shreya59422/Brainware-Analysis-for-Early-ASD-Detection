Purpose
This script evaluates the trained model performance.

What it does: loads dataset
              loads trained model
              predicts ASD
              calculates metrics
              prints accuracy, precision, recall, F1 score
