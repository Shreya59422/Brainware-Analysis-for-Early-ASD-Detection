Purpose
This script trains the machine learning model using the EEG dataset.

What it does: loads dataset
              cleans missing values
              creates ASD label
              selects EEG features
              scales data
              balances data with SMOTE
              trains XGBoost
              saves the trained model
